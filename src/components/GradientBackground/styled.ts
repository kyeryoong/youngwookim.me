'use client';
import styled from 'styled-components';

export const Wrapper = styled('header')`
  width: 100vw;
  height: 100vh;
  position: fixed;
  top: 0px;
  left: 0px;
  z-index: 1;

  .gradient {
    width: 100%;
    height: 100%;
    z-index: 1;
  }
`;
