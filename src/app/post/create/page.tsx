import CreatePage from '@/components/Pages/Post/CreatePage';
import Page from '@/theme/Page';

const CreatePostPage = () => {
  return (
    <Page>
      <CreatePage mode={'create'} />
    </Page>
  );
};

export default CreatePostPage;
